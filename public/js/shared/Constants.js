/**
 * ============================================================================
 * Constants - 客户端常量
 * ============================================================================
 *
 * 客户端渲染所需的所有常量定义。
 * 与服务端 GameConfig.js 保持同步的值（如 WALL/FLOOR/tileSize）通过
 * gameStart 事件从服务端下发，这里仅定义客户端独立使用的常量。
 *
 * 颜色值采用 Limbo 风格的极暗色调，修改此处可快速调整整体视觉风格。
 */

const CONSTANTS = {
  // ──────────────────────────────────────────────
  // 瓦片类型（需与服务端 GameConfig.map 一致）
  // ──────────────────────────────────────────────
  WALL: 1,
  FLOOR: 0,

  // ──────────────────────────────────────────────
  // 角色标识（需与服务端 GameConfig.room.roles 一致）
  // ──────────────────────────────────────────────
  ROLES: {
    BLIND: 'blind',
    HELPER: 'helper',
  },

  // 默认瓦片大小（实际值由 gameStart 事件覆盖）
  TILE_SIZE: 40,

  // 玩家参数（与服务端同步）
  PLAYER_RADIUS: 12,
  PLAYER_SPEED: 3,

  // ──────────────────────────────────────────────
  // 颜色配置 - Limbo 风格暗色调
  // ──────────────────────────────────────────────
  COLORS: {
    // 帮助者端地图
    wall: '#0a0a0a',          // 墙壁主体色（接近纯黑）
    wallEdge: '#1a1a1a',      // 墙壁边缘高光（微弱可见）
    floor: '#0d0d0d',         // 地板底色
    floorDetail: '#111111',   // 地板细节纹理色

    // 盲人端
    blindBg: '#000000',       // 纯黑背景
    glowColor: 'rgba(255, 255, 255, 0.08)',   // 外层光晕
    glowCore: 'rgba(255, 255, 255, 0.15)',    // 核心光圈
    particleColor: 'rgba(255, 255, 255, 0.03)', // 环境颗粒

    // 帮助者端
    helperBg: '#050505',                       // 帮助者端背景
    playerGlow: 'rgba(255, 255, 255, 0.9)',    // 玩家光点核心
    playerGlowOuter: 'rgba(255, 255, 255, 0.2)', // 玩家光点外发光
    hurtOverlay: 'rgba(180, 20, 20, 0.35)',    // 受伤叠加色
  },
};
