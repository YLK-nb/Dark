/**
 * ============================================================================
 * GameConfig - 全局配置
 * ============================================================================
 *
 * 服务端和客户端共享的游戏常量。
 * 客户端通过 <script> 标签加载 src/config/GameConfig.js（用于服务端逻辑）
 * 或使用 public/js/shared/Constants.js（纯客户端常量，如颜色）。
 *
 * 修改此文件会影响：
 * - 地图生成尺寸和瓦片大小
 * - 玩家移动速度和碰撞半径
 * - 网络同步频率
 * - 房间最大人数
 */

const GameConfig = {
  // ──────────────────────────────────────────────
  // 服务器
  // ──────────────────────────────────────────────
  server: {
    port: 3001,       // 监听端口
  },

  // ──────────────────────────────────────────────
  // 地图
  // ──────────────────────────────────────────────
  map: {
    mazeSize: 8,      // 迷宫格子数 (NxN)，越大地图越复杂
    cellSize: 5,      // 每个格子扩展为 MxM 瓦片，控制通道宽度
    tileSize: 40,     // 单个瓦片的像素大小
    WALL: 1,          // 墙壁瓦片值
    FLOOR: 0,         // 地板瓦片值
  },

  // ──────────────────────────────────────────────
  // 玩家
  // ──────────────────────────────────────────────
  player: {
    speed: 3,         // 每帧移动像素数 (在 ~30fps 下约为 90px/s)
    radius: 12,       // 碰撞检测圆形半径 (像素)
    startX: 1,        // 起始瓦片坐标 X（实际位置由 MapGenerator 确定）
    startY: 1,        // 起始瓦片坐标 Y
  },

  // ──────────────────────────────────────────────
  // 房间
  // ──────────────────────────────────────────────
  room: {
    maxPlayers: 2,    // 每房间最大玩家数（1 盲人 + 1 帮助者）
    roles: {
      BLIND: 'blind',   // 盲人角色标识
      HELPER: 'helper', // 帮助者角色标识
    },
  },

  // ──────────────────────────────────────────────
  // 网络
  // ──────────────────────────────────────────────
  syncInterval: 33,   // 位置广播间隔 (ms)，33ms ≈ 30fps
};

// Node.js 和浏览器兼容导出
if (typeof module !== 'undefined' && module.exports) {
  module.exports = GameConfig;
}
